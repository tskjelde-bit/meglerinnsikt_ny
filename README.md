# Meglerinnsikt

Interaktivt boligmarkedskart for Oslo — bydel for bydel.

## Utvikling

```bash
npm install
npm run dev
```

Krever `NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN` i `.env.local`.

## Deploy

Pusher til `main` → GitHub Actions bygger og deployer til GitHub Pages.
